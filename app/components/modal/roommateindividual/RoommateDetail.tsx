'use client';

interface RoommateDetailProps {
  price: number;
  movedate: string;
  length: string;
}

const RoommateDetail: React.FC<RoommateDetailProps> = ({
  price,
  movedate,
  length,
}) => {
  return <div></div>;
};
export default RoommateDetail;
